export const DATA_URL = "https://reqres.in/api/products";

export const numberOfItemsToDisplayPerPage = 5;

export const columns = [{ field: "Id" }, { field: "Name" }, { field: "Year" }];

export const colors = {
  dark: "#272522",
  btnColor: "#c4bbab",
};
